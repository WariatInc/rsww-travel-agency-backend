from setuptools import setup

setup(
    name="tour_operator_service",
    version="1.0.0",
    description="Tour operator service",
    include_package_data=True,
    zip_safe=False,
    packages=["src"],
)
