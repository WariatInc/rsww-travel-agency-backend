from setuptools import setup

setup(
    name="trip_offer_service",
    version="1.0.0",
    description="Trip Offer Service",
    include_package_data=True,
    zip_safe=False,
    packages=["src"],
)
